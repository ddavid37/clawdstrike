# Generic Adapter Examples

This folder demonstrates the framework-agnostic `@clawdstrike/adapter-core` wrapper.

Files:

- `sample-app.ts`: minimal app-style tool dispatcher
- `server-handler.ts`: server-side request handler pattern

These examples are intentionally framework-neutral. Use them as templates for custom integrations.
